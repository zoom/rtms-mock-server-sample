
const HANDSHAKE_PORT = 9092;
const MEDIA_STREAM_PORT = 8081;
const KEEP_ALIVE_INTERVAL = 5000;
const STREAM_CHUNK_SIZE = 4096;
const AUDIO_INTERVAL_MS = 100;

// Export all constants and enums from the original file
module.exports = {
    HANDSHAKE_PORT,
    MEDIA_STREAM_PORT,
    KEEP_ALIVE_INTERVAL,
    STREAM_CHUNK_SIZE,
    AUDIO_INTERVAL_MS,
    // Add all other constants from the original file
};
